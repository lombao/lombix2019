./configure --prefix=$PREFIX  				\
            --exec-prefix=                              \
            --libdir=$PREFIX/lib                        \
            --bindir=$PREFIX/bin                        \
            --sbindir=$PREFIX/sbin                       \
            --disable-static                         	\
            --disable-kill

