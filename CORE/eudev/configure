

./configure --prefix=$PREFIX           		\
            --build=$LMBX_BUILD			\
            --bindir=$PREFIX/bin       		\
            --sbindir=$PREFIX/sbin		\
            --libdir=$PREFIX/lib       		\
            --with-rootrundir=/run     		\
            --with-rootlibdir=$PREFIX/lib  	\
            --enable-manpages       		\
            --disable-static        		\
            --disable-selinux       		\
            --config-cache

