make install

#
# I am used to issue "crontab" 
ln -svf $PREFIX/bin/fcrontab $PREFIX/bin/crontab

strip --strip-unneeded $PREFIX/bin/fcrontab
strip --strip-unneeded $PREFIX/bin/fcrondyn
strip --strip-unneeded $PREFIX/bin/fcronsighup
strip --strip-unneeded $PREFIX/sbin/fcron
